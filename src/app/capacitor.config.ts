import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.iceinstantshare.app',
  appName: 'instantshare',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
